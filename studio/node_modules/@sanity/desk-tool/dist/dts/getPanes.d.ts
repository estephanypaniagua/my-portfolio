import { PaneNode, RouterPaneGroup } from './types';
import { LOADING_PANE } from './constants';
interface PaneData {
    active: boolean;
    childItemId: string | null;
    groupIndex: number;
    index: number;
    itemId: string;
    key: string;
    pane: PaneNode | typeof LOADING_PANE;
    params: Record<string, string>;
    path: string;
    payload: unknown;
    selected: boolean;
    siblingIndex: number;
}
export declare function getPanes(resolvedPanes: Array<PaneNode | typeof LOADING_PANE>, routerPanes: RouterPaneGroup[]): PaneData[];
export {};
//# sourceMappingURL=getPanes.d.ts.map